{
  "name": "mailgent-cli",
  "version": "0.2.0",
  "description": "CLI for the Mailgent API — identity infrastructure for AI agents",
  "bin": { "mailgent": "dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run"
  },
  "engines": { "node": ">=18" },
  "keywords": ["mailgent", "cli", "ai", "agent", "email"],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
